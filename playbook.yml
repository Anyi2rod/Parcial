---
- hosts: all
  become: yes

  tasks:
    - name: Instalar nginx
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Copiar página personalizada
      copy:
        content: "<h1>Hola soy Anyi desde Ansible!</h1>"
        dest: /var/www/html/index.html
